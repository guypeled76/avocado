

extend type Query {
    userById(userId:ID!): User!
}

type User {
    id: ID!
    name: String!
    email: String!
    image: String!
    hashtags:[String!]!
    notifications: [Notification!]!
    profile: Profile!
}

type Profile {
    id: ID!
}