extend type Mutation {
    createHashTag(name:String!): HashTag

}

extend type Query {
    hashTags(filter:ResultsFilter) : [HashTag!]!
}


type HashTag {
    id:ID!
    name:String!
    createdAt: Timestamp!
    updatedAt: Timestamp!
    deletedAt: Timestamp
}