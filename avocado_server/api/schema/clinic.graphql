

extend type Query {
    clinicById(clinicID:ID!): Clinic!
    clinics:[Clinic!]!
}

type Clinic {
    id: ID!
    name: String!
    createdBy: ID!
    createdAt: Timestamp!
    updatedAt: Timestamp!
    deletedAt: Timestamp
}
