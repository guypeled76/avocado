
extend type Query {
    notificationsByUserId(userID:ID!): [Notification!]!
}

type Notification {
    id: ID!
    text: String!
    Resource:Resource!
    reference:Reference!
    createdAt:Timestamp!
    createdBy:User
}