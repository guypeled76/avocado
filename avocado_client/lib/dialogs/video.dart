import 'package:flutter/material.dart';

class VideoDialog extends StatelessWidget {
  VideoDialog({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("video"),
        ),
        body: Text("video")
    );
  }
}